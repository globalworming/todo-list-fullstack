{
  "devDependencies": {
    "@playwright/test": "^1.31.2",
    "playwright": "^1.31.2",
    "prettier": "2.8.7"
  },
  "scripts": {
    "test-prod": "PLAYWRIGHT_JUNIT_OUTPUT_NAME=junit.xml BASE_URL=https://single-page-application-fg5blhx72q-ey.a.run.app/ npx playwright test --reporter=junit,html",
    "test-local": "PLAYWRIGHT_JUNIT_OUTPUT_NAME=junit.xml BASE_URL=http://localhost:3000 npx playwright test --reporter=junit,html",
    "prettier": "prettier --write ."
  },
  "prettier": {
    "printWidth": 120,
    "singleQuote": true
  }
}
